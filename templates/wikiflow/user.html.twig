{% extends 'base.html.twig' %}

{% block body %}
    <h2> Users :</h2>


        <table class="table">
            <thead>
            <th>Prénom :</th>
            <th>Nom :</th>
            <th>Email :</th>
            <th>Ville :</th>
            </thead>
    {% for user in users %}
            <tbody>
            <td>{{ user.firstname }}</td>
            <td>{{ user.lastname }}</td>
            <td>{{ user.email }}</td>
            {% for adress in adresses %}
            {% if user.addressId == adress.id %}
            <td>{{ adress.city }}</td>
            {% endif %}
            {% endfor %}
            </tbody>
    {% endfor %}

        </table>

{% endblock %}
